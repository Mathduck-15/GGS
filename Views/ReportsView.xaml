<UserControl x:Class="GoodGovernanceApp.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodels="clr-namespace:GoodGovernanceApp.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">

    <UserControl.DataContext>
        <viewmodels:ReportsViewModel />
    </UserControl.DataContext>

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="System Reports &amp; Analytics" FontSize="28" FontWeight="Light" Margin="0 0 0 24"/>

        <!-- Controls Area -->
        <materialDesign:Card Grid.Row="1" Margin="0 0 0 16" Padding="16">
            <StackPanel Orientation="Horizontal">
                <ComboBox materialDesign:HintAssist.Hint="Select Report Type" 
                          ItemsSource="{Binding ReportTypes}" 
                          SelectedItem="{Binding SelectedReportType}" 
                          Width="300" Margin="0 0 16 0" VerticalAlignment="Center" />
                
                <Button Content="REFRESH DATA" Command="{Binding GenerateReportCommand}" 
                        Style="{StaticResource MaterialDesignOutlinedButton}" Margin="0 0 8 0" />
                        
                <Button Content="EXPORT / PRINT" Command="{Binding PrintExportCommand}" 
                        Style="{StaticResource MaterialDesignFlatButton}" Foreground="{DynamicResource PrimaryHueMidBrush}" />
            </StackPanel>
        </materialDesign:Card>

        <!-- Report Display Area -->
        <materialDesign:Card Grid.Row="2" Padding="16">
            <Grid>
                <!-- 1. User Activity Log -->
                <DataGrid ItemsSource="{Binding UserActivityLogs}" AutoGenerateColumns="False" IsReadOnly="True" 
                          Visibility="{Binding IsUserActivityLogVisible, Converter={StaticResource BoolToVis}}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp, StringFormat={}{0:G}}" />
                        <DataGridTextColumn Header="User" Binding="{Binding User.Username}" />
                        <DataGridTextColumn Header="Action" Binding="{Binding Action}" />
                        <DataGridTextColumn Header="Details" Binding="{Binding Details}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>

                <!-- 2. Budget Summary by Category -->
                <DataGrid ItemsSource="{Binding BudgetSummaries}" AutoGenerateColumns="False" IsReadOnly="True" 
                          Visibility="{Binding IsBudgetSummaryVisible, Converter={StaticResource BoolToVis}}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Category" Binding="{Binding CategoryName}" />
                        <DataGridTextColumn Header="Total Allocated Budget" Binding="{Binding TotalBudget, StringFormat={}{0:C}}" />
                        <DataGridTextColumn Header="Total Expenses" Binding="{Binding TotalExpenses, StringFormat={}{0:C}}" />
                        <DataGridTextColumn Header="Remaining Balance" Binding="{Binding RemainingBalance, StringFormat={}{0:C}}" />
                    </DataGrid.Columns>
                </DataGrid>

                <!-- 3. Transaction History -->
                <DataGrid ItemsSource="{Binding TransactionHistory}" AutoGenerateColumns="False" IsReadOnly="True" 
                          Visibility="{Binding IsTransactionHistoryVisible, Converter={StaticResource BoolToVis}}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat={}{0:d}}" />
                        <DataGridTextColumn Header="Type" Binding="{Binding TransactionType}" />
                        <DataGridTextColumn Header="Category" Binding="{Binding Category.Name}" />
                        <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat={}{0:C}}" />
                        <DataGridTextColumn Header="User" Binding="{Binding User.Username}" />
                        <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>

                <!-- 4. Parameters List -->
                <DataGrid ItemsSource="{Binding ParametersList}" AutoGenerateColumns="False" IsReadOnly="True" 
                          Visibility="{Binding IsParametersListVisible, Converter={StaticResource BoolToVis}}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Parameter Name" Binding="{Binding Name}" />
                        <DataGridTextColumn Header="Current Value" Binding="{Binding Value}" />
                        <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>

                <!-- 5. Departmental Budget Allocation -->
                <DataGrid ItemsSource="{Binding DepartmentalBudgets}" AutoGenerateColumns="False" IsReadOnly="True" 
                          Visibility="{Binding IsDepartmentalBudgetVisible, Converter={StaticResource BoolToVis}}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Department" Binding="{Binding DepartmentName}" Width="*" />
                        <DataGridTextColumn Header="Year" Binding="{Binding Year}" />
                        <DataGridTextColumn Header="Allocated Amount" Binding="{Binding Allocated, StringFormat={}{0:C}}" />
                        <DataGridTextColumn Header="Spent Amount" Binding="{Binding Spent, StringFormat={}{0:C}}" />
                        <DataGridTextColumn Header="Remaining" Binding="{Binding Remaining, StringFormat={}{0:C}}" />
                        <DataGridTextColumn Header="Utilization (%)" Binding="{Binding Utilization, StringFormat={}{0:F2}%}" />
                    </DataGrid.Columns>
                </DataGrid>

                <!-- 6. System Overview -->
                <DataGrid ItemsSource="{Binding SystemOverview}" AutoGenerateColumns="False" IsReadOnly="True" 
                          Visibility="{Binding IsSystemOverviewVisible, Converter={StaticResource BoolToVis}}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Key Metric" Binding="{Binding Metric}" />
                        <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
                
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>
