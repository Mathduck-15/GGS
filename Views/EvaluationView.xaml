<UserControl x:Class="GoodGovernanceApp.Views.EvaluationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodels="clr-namespace:GoodGovernanceApp.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100">

    <UserControl.DataContext>
        <viewmodels:EvaluationViewModel />
    </UserControl.DataContext>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="File Evaluation Center" FontSize="28" FontWeight="Light" Margin="0 0 0 24"   Foreground="#000000"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- Pending Files List -->
            <materialDesign:Card Grid.Column="0" Margin="0 0 12 0">
                <Grid Margin="24">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Pending Reviews" FontSize="22" FontWeight="Bold"  Foreground="#000000" 
               Margin="0 0 0 0"/>

                    <DataGrid Grid.Row="2" ItemsSource="{Binding PendingFiles}" SelectedItem="{Binding SelectedFile}" 
              AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" 
              SelectionMode="Single" RowHeight="40" ColumnHeaderHeight="40"
              BorderThickness="1" GridLinesVisibility="Horizontal">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="File Name" Binding="{Binding FileName}" Width="2*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Padding" Value="8 0"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Office" Binding="{Binding Office.Name}" Width="*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Padding" Value="8 0"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Category" Binding="{Binding Category.Name}" Width="*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Padding" Value="8 0"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Type" Binding="{Binding FileType}" Width="Auto">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Padding" Value="8 0"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Date" Binding="{Binding UploadDate, StringFormat={}{0:d}}" Width="Auto">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Padding" Value="8 0"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </materialDesign:Card>

            <!-- Evaluation Form -->
            <materialDesign:Card Grid.Column="1" Margin="12 0 0 0" VerticalAlignment="Top">
                <StackPanel Margin="16">
                    <TextBlock Text="Evaluation Form" FontSize="18" FontWeight="Bold" Margin="0 0 0 16"  Foreground="#000000"/>

                    <TextBlock Text="Evaluating:" Foreground="#000000"/>
                    <TextBlock Text="{Binding SelectedFile.FileName}" FontSize="16" FontWeight="SemiBold" Margin="0 0 0 24"/>

                    <Button Content="READ FILE" Command="{Binding OpenFileCommand}" 
                            Style="{StaticResource MaterialDesignOutlinedButton}" 
                            materialDesign:ButtonAssist.CornerRadius="4"
                            Margin="0 0 0 24">
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="FileEye" Margin="0 0 8 0"/>
                                    <TextBlock Text="Open for Review"/>
                                </StackPanel>
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>

                    <Separator Margin="0 0 0 24"/>

                    <TextBlock Text="Score (0 - 100)" Margin="0 0 0 8"  Foreground="#000000"/>
                    <Slider Minimum="0" Maximum="100" Value="{Binding Score}" TickFrequency="5" IsSnapToTickEnabled="True" Margin="0 0 0 24"/>
                    <TextBlock Text="{Binding Score}" HorizontalAlignment="Center" FontSize="20" FontWeight="Bold" Margin="0 -20 0 16"/>

                    <TextBox materialDesign:HintAssist.Hint="Reviewer Comments" 
                             Text="{Binding Comments}" 
                             Style="{StaticResource MaterialDesignOutlinedTextBox}" 
                             Height="120" TextWrapping="Wrap" AcceptsReturn="True"
                              Foreground="#000000"
                             Margin="0 0 0 24"/>

                    <Button Content="SUBMIT SCORE" Command="{Binding SubmitEvaluationCommand}" Style="{StaticResource MaterialDesignRaisedButton}"/>
                </StackPanel>
            </materialDesign:Card>
        </Grid>
    </Grid>
</UserControl>
