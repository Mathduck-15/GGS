<Window x:Class="GoodGovernanceApp.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:GoodGovernanceApp.Views"
        mc:Ignorable="d"
        Title="Good Governance Management - Login" Height="450" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Name="LoginWin">
    
    <Grid>
        <materialDesign:Card Width="300" VerticalAlignment="Center" Padding="16">
            <StackPanel>
                <TextBlock Text="Welcome Back" FontSize="24" FontWeight="Bold" Margin="0 0 0 8" HorizontalAlignment="Center"/>
                <TextBlock Text="Login to your account" FontSize="14" Foreground="Gray" Margin="0 0 0 24" HorizontalAlignment="Center"/>
                
                <TextBox materialDesign:HintAssist.Hint="Username" Style="{StaticResource MaterialDesignFloatingHintTextBox}" 
                         Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0 0 0 16" />
                
                <PasswordBox materialDesign:HintAssist.Hint="Password" Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" 
                             Name="PasswordBoxControl" Margin="0 0 0 8" PasswordChanged="PasswordBox_PasswordChanged"/>
                
                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0 0 0 16" HorizontalAlignment="Center"/>
                
                <Button Content="LOGIN" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=LoginWin}" 
                        Style="{StaticResource MaterialDesignRaisedButton}" Margin="0 8 0 0"/>
                
                <Button Content="Database Connection Settings" Command="{Binding OpenDbSettingsCommand}" 
                        Style="{StaticResource MaterialDesignFlatButton}" Margin="0 16 0 0" 
                        Foreground="{DynamicResource PrimaryHueMidBrush}" />
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</Window>
