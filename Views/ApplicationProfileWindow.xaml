<Window x:Class="GoodGovernanceApp.Views.ApplicationProfileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Application Profile" Height="500" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Application Profile" FontSize="20" FontWeight="Bold" Margin="0 0 0 16" HorizontalAlignment="Center"/>

        <TextBox Grid.Row="1" materialDesign:HintAssist.Hint="Governance Name" 
                 Text="{Binding GoveName, UpdateSourceTrigger=PropertyChanged}" 
                 Style="{StaticResource MaterialDesignFloatingHintTextBox}" 
                 Margin="0 0 0 16" FontSize="16"/>

        <TextBox Grid.Row="2" materialDesign:HintAssist.Hint="Address" 
                 Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" 
                 Style="{StaticResource MaterialDesignFloatingHintTextBox}" 
                 Margin="0 0 0 16" FontSize="16"/>

        <Grid Grid.Row="3" Margin="0 0 0 16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" materialDesign:HintAssist.Hint="Logo Path" 
                     Text="{Binding LogoAddress}" 
                     IsReadOnly="True"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}" 
                     Margin="0 0 8 0"/>
            <Button Grid.Column="1" Content="Browse" Command="{Binding BrowseCommand}" VerticalAlignment="Bottom"/>
        </Grid>

        <Border Grid.Row="4" BorderBrush="LightGray" BorderThickness="1" CornerRadius="4" Margin="0 0 0 16" Background="WhiteSmoke">
            <Grid>
                <TextBlock Text="No Logo Selected" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Gray"/>
                <Image Source="{Binding LogoPreview}" Stretch="Uniform" Margin="8"/>
            </Grid>
        </Border>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save Options" Command="{Binding SaveCommand}" Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>
    </Grid>
</Window>
