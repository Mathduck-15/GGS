<UserControl x:Class="GoodGovernanceApp.Views.BudgetAllocationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodels="clr-namespace:GoodGovernanceApp.ViewModels"
             xmlns:converters="clr-namespace:GoodGovernanceApp.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">

    <UserControl.Resources>
        <converters:AllocationColorConverter x:Key="AllocationColorConverter" />
        <converters:ProgressColorConverter x:Key="ProgressColorConverter" />
    </UserControl.Resources>

    <UserControl.DataContext>
        <viewmodels:BudgetAllocationViewModel />
    </UserControl.DataContext>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Annual Budget Allocation" FontSize="28" FontWeight="Light" Margin="0 0 0 24"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" MinWidth="350"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <!-- Yearly Budgets Management -->
            <StackPanel Grid.Column="0" Margin="0 0 12 0">
                <materialDesign:Card Padding="16" Margin="0 0 0 16">
                    <StackPanel>
                        <TextBlock Text="New Yearly Budget" FontSize="18" FontWeight="Bold" Margin="0 0 0 16"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="2*"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0" materialDesign:HintAssist.Hint="Year" Text="{Binding NewBudgetYear}" Margin="0 0 8 16"/>
                            <TextBox Grid.Column="1" materialDesign:HintAssist.Hint="Total Amount" Text="{Binding NewBudgetAmount}" Margin="0 0 0 16"/>
                        </Grid>
                        <TextBox materialDesign:HintAssist.Hint="Description" Text="{Binding NewBudgetDescription}" Margin="0 0 0 16"/>
                        <Button Content="Set Yearly Budget" Command="{Binding AddYearlyBudgetCommand}" Style="{StaticResource MaterialDesignRaisedButton}"/>
                    </StackPanel>
                </materialDesign:Card>

                <materialDesign:Card VerticalAlignment="Stretch">
                    <Grid Margin="16">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Available Budgets" FontSize="18" FontWeight="Bold" Margin="0 0 0 12"/>
                        <ListBox Grid.Row="1" ItemsSource="{Binding YearlyBudgets}" SelectedItem="{Binding SelectedYearlyBudget}" MinHeight="200">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0 4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Year}" FontSize="16" FontWeight="SemiBold"/>
                                        <TextBlock Grid.Column="1" Text="{Binding TotalAmount, StringFormat={}{0:C}}" VerticalAlignment="Center" Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </materialDesign:Card>
            </StackPanel>

            <!-- Department Allocations -->
            <materialDesign:Card Grid.Column="1" Margin="12 0 0 0">
                <Grid Margin="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Budget Distribution" FontSize="20" FontWeight="Bold" Margin="0 0 0 16"/>

                    <!-- Summary Stats -->
                    <Grid Grid.Row="1" Margin="0 0 0 24">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Total Yearly Budget" Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                            <TextBlock Text="{Binding SelectedYearlyBudget.TotalAmount, StringFormat={}{0:C}}" FontSize="24" FontWeight="SemiBold"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Remaining to Allocate" Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                            <TextBlock Text="{Binding UnallocatedAmount, StringFormat={}{0:C}}" FontSize="24" FontWeight="SemiBold" 
                                       Foreground="{Binding UnallocatedAmount, Converter={StaticResource AllocationColorConverter}}"/>
                        </StackPanel>
                    </Grid>

                    <DataGrid Grid.Row="2" ItemsSource="{Binding DepartmentAllocations}" AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Single">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Department" Binding="{Binding DepartmentName}" IsReadOnly="True" Width="*"/>
                            <DataGridTemplateColumn Header="Allocated Amount" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Grid.Row="3" Margin="0 16 0 0">
                        <ProgressBar Value="{Binding AllocationPercent}" Height="10" Margin="0 0 0 16" 
                                     Foreground="{Binding UnallocatedAmount, Converter={StaticResource ProgressColorConverter}}"/>
                        <Button Content="Save Allocations" Command="{Binding SaveAllocationsCommand}" Style="{StaticResource MaterialDesignRaisedButton}"/>
                    </StackPanel>
                </Grid>
            </materialDesign:Card>
        </Grid>
    </Grid>
</UserControl>
